minimum-bulk: v0.4.5

versions:

- file: Cargo.toml
  block-start: ^\[package\]
  block-end: ^\[.*\]
  regex: ^version\s*=\s*"(\S+)"

- file: Cargo.lock
  block-start: ^name\s*=\s*"nginx-config-mod"
  regex: ^version\s*=\s*"(\S+)"
  block-end: ^\[.*\]
